<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sndebayankumar.buildinginformationofjust">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BSMA_Activity"
            android:label="BSMABuilding"/>
        <activity android:name=".LBActivity"
            android:label="LibraryBuilding"/>
        <activity android:name=".MBActivity"
            android:label="MedicalBuilding"/>
        <activity android:name=".AB_Activity"
            android:label="AdministrativeBuilding"/>
        <activity android:name=".SMRH_Activity"
            android:label="SMRHall"/>
        <activity android:name=".SHH_Activity"
            android:label="SHHall">

        </activity>
    </application>

</manifest>